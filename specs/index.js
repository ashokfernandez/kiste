var describe = global.describe
var it = global.it
var expect = require('chai').expect

describe('When I run this test', function () {
  it('It should pass', function () {
    expect(true).to.eq(true)
  })
})
